#include <cstdio>

// TODO: judge this code

int main() {
    int total_cas;
    scanf("%d", &total_cas);
    for (int cas = 1; cas <= total_cas; cas++) {
        printf("Case #%d: IMPOSSIBLE\n", cas);
    }
    return 0;
}
